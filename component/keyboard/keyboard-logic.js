export function keyboardLogic(){
     const d = document,
          keyInput = d.querySelectorAll('.keyboard-container div')

     // keyInput.forEach(k => console.log(k))

     d.addEventListener()
}