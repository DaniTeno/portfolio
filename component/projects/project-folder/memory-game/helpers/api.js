const SITE = "https://pokeapi.co/api/v2/",
     POKEMONS = `${SITE}pokemon/?limit=40`,
     POKEMON = `${SITE}pokemon/`,
     ITEMS = `${SITE}item/`,
     REGIONS = `${SITE}region/`,
     BERRIES = `${SITE}/berry`,
     TYPES = `${SITE}/type`;

export default {
     SITE,
     POKEMONS,
     POKEMON,
     ITEMS,
     REGIONS,
     BERRIES,
     TYPES
}